"""
MCP server exposing OpenAI gpt-image-2 image generation and editing.

Public surface intentionally minimal — the package is meant to be invoked
as a CLI (`mcp-openai-images-audio`) or as a module (`python -m
mcp_openai_images_audio`); importing internals is not a supported
contract.
"""

__version__ = "0.1.0"
